// NOTES:
//
// 1. Compile using `group-testrust-rust-rustc main.rs --out-dir $(group-testrust-common-pwd-build)`.
// 2. Run using `$(group-testrust-common-pwd-build)/main`.
//
// This can be improved upon in the future as the need arises.

fn main() {
    println!("Hello, Rust!");
}
